# LLD Log — Arjit (C++)

Each problem gets its own folder with real code + a short design note. This README is the index.

| Date Started | Problem | Folder | Status | Notes |
|--------------|---------|--------|--------|-------|
| -            | -       | -      | -      | -     |

Folder layout:
```
lld/
├── README.md
└── <problem-name>/
    ├── main.cpp / *.cpp / *.h
    └── notes.md      → design decisions, classes, tradeoffs
```

Same problems as Sreehitha's `s_space/lld/` — pick from a shared list, solve independently, compare approaches after.
